import { css } from '@emotion/react';

const buttonReset = css(css`
  appearance: none;
  background: transparent;
  border: 0;
  border-radius: 0;
  box-shadow: none;
  cursor: pointer;
  font-family: inherit;
  font-size: inherit;
  padding: 0;
`);

export default buttonReset;
